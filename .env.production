# VITE_API_BASE_URL must point to the deployed backend.
# For the Docker/Render deployment, the frontend is served by nginx
# alongside the backend, so relative URLs work automatically.
# For standalone Vercel deployment, set this to the Render backend URL.
VITE_API_BASE_URL=https://learnatwill.onrender.com/
